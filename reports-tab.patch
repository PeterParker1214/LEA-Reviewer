diff --git a/admin.html b/admin.html
index 58bd02e..e2f3587 100644
--- a/admin.html
+++ b/admin.html
@@ -218,6 +218,53 @@
   .report-card .rc-reasons{font-size:12.5px;color:var(--muted);margin-bottom:6px;line-height:1.6;}
   .report-card .rc-reasons b{color:var(--ink);}
   .report-card .rc-comment{font-size:12.5px;color:var(--ink);background:var(--bg-panel);border-radius:6px;padding:8px 10px;margin:4px 0;}
+
+  /* ===== Reports tab =====
+     A report opens where it sits. The old card had an "Edit question" button
+     that threw you into the Pages tab and left you to find the question
+     yourself; everything needed to settle a report is now in the card. */
+  .rep-head-strip{display:flex;align-items:baseline;justify-content:space-between;gap:12px;flex-wrap:wrap;margin-bottom:14px;}
+  .rep-head-strip h2{font-family:var(--font-body);font-size:17px;font-weight:600;margin:0;}
+  .rep-head-strip .note{font-size:13px;color:var(--muted);}
+  .rep-chips{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:18px;}
+  .rep-chip{font-size:13px;padding:6px 12px;border-radius:20px;background:transparent;}
+  .rep-chip.on{background:var(--gold);color:var(--bg-deep);border-color:var(--gold);font-weight:600;}
+
+  .rep{border:1px solid var(--line);border-radius:12px;background:var(--bg-panel-deep);margin-bottom:14px;overflow:hidden;}
+  .rep.open{border-color:var(--gold);}
+  .rep.done{opacity:.55;}
+  .rep-head{display:flex;gap:14px;align-items:flex-start;padding:16px;cursor:pointer;}
+  .rep-head:hover{background:rgba(111,168,207,0.05);}
+  .rep-caret{color:var(--muted);font-size:13px;line-height:1.5;flex-shrink:0;width:12px;transition:transform .15s ease;}
+  .rep.open .rep-caret{transform:rotate(90deg);}
+  .rep-main{flex:1;min-width:0;}
+  .rep-where{font-size:12.5px;color:var(--muted);margin-bottom:5px;}
+  .rep-q{font-size:14.5px;line-height:1.5;margin-bottom:8px;}
+  .rep-meta{display:flex;gap:8px;align-items:center;flex-wrap:wrap;font-size:12.5px;color:var(--muted);}
+  .rep-meta .by b{color:var(--ink);font-weight:600;}
+  .rep-tag{font-size:12px;padding:3px 10px;border-radius:20px;border:1px solid var(--line);}
+  .rep-tag.key{color:var(--bad);border-color:var(--bad);background:rgba(226,96,74,.12);}
+  .rep-tag.wording{color:var(--gold-bright);border-color:var(--gold);background:rgba(224,168,63,.12);}
+
+  .rep-body{padding:0 16px 16px;border-top:1px solid var(--line);}
+  .rep-sec{margin-top:18px;}
+  .rep-sec-title{font-size:13px;font-weight:600;margin-bottom:8px;}
+  .rep-sec-title span{color:var(--muted);font-weight:400;}
+  .rep-said{background:var(--bg-panel);border-radius:8px;padding:12px 14px;font-size:13.5px;line-height:1.6;margin-bottom:8px;}
+  .rep-said .who{display:block;font-size:12px;color:var(--muted);margin-bottom:6px;}
+  .rep-choice{display:flex;gap:10px;align-items:center;margin-bottom:8px;}
+  .rep-choice input[type="radio"]{width:17px;height:17px;flex-shrink:0;accent-color:var(--good);cursor:pointer;}
+  .rep-choice input[type="text"]{flex:1;}
+  .rep-choice.correct input[type="text"]{border-color:var(--good);}
+  .rep-choice .mark{font-size:12px;color:var(--good);width:56px;flex-shrink:0;}
+  .rep-hint{font-size:12.5px;color:var(--muted);margin:6px 0 0;line-height:1.55;}
+  .rep-btns{display:flex;gap:8px;flex-wrap:wrap;margin-top:20px;}
+  .rep-btns .spacer{flex:1;}
+  .rep-reply{margin-top:12px;}
+  @media (max-width:560px){
+    .rep-head{padding:14px;gap:10px;}
+    .rep-choice .mark{display:none;}
+  }
 </style>
 </head>
 <body>
@@ -552,6 +599,20 @@ let openReports = [];          // raw rows, status='open'
 let reportsByQuestionRef = {}; // question_ref -> [report, ...]
 let reportsLoadError = null;
 
+let reporterNames = {};   // user_id -> username, filled from `profiles`
+
+// reports.user_id has its foreign key on auth.users, not profiles, so
+// PostgREST will not join the two in one select. The names come from a second
+// read of profiles, which is publicly readable.
+async function loadReporterNames(){
+  const ids = [...new Set(openReports.map(r => r.user_id).filter(Boolean))]
+    .filter(id => !(id in reporterNames));
+  if(!ids.length) return;
+  const { data } = await sb.from('profiles').select('id,username').in('id', ids);
+  (data || []).forEach(p => { reporterNames[p.id] = p.username; });
+}
+function reporterName(r){ return reporterNames[r.user_id] || 'someone'; }
+
 async function loadReports(){
   const { data, error } = await sb.from('reports').select('*').eq('status', 'open').order('created_at', { ascending: false });
   // A failed query used to fall through to `data || []`, which renders the
@@ -561,6 +622,7 @@ async function loadReports(){
   openReports = data || [];
   reportsByQuestionRef = {};
   openReports.forEach(r => { (reportsByQuestionRef[r.question_ref] = reportsByQuestionRef[r.question_ref] || []).push(r); });
+  await loadReporterNames();
 }
 // Report counts for whatever file is currently open in the Pages editor, keyed by question index.
 function reportCountsForCurrentFile(){
@@ -2262,16 +2324,71 @@ async function handleAddModule(){
 
 /* ================= REPORTS TAB ================= */
 
-function reasonSummary(reports){
-  const counts = {};
-  reports.forEach(r => { counts[r.reason] = (counts[r.reason] || 0) + 1; });
-  return Object.keys(counts).map(reason => (counts[reason] > 1 ? counts[reason] + '× ' : '') + reason).join(', ');
+// The reasons the reader-facing form offers are phrased for a reader mid-quiz.
+// On this screen they are labels on a to-do list, so they get shorter names.
+const REASON_LABELS = {
+  'Answer key looks wrong': { text: 'Wrong answer', cls: 'key' },
+  'Typo or unclear wording': { text: 'Wording', cls: 'wording' }
+};
+function reasonLabel(reason){
+  return REASON_LABELS[reason] || { text: reason, cls: 'wording' };
+}
+function shortWhen(iso){
+  const d = new Date(iso);
+  return isNaN(d) ? '' : d.toLocaleDateString(undefined, { day: 'numeric', month: 'short' });
+}
+
+let reportOpenRef = null;   // the one report card expanded right now
+let reportFilter = 'all';   // 'all', or a reason string
+const reportFileCache = {}; // module file path -> { raw, source }
+const reportQ = {};         // question_ref -> { file, qIndex, row, where } or { error }
+
+async function fetchModuleSource(file){
+  if(reportFileCache[file]) return reportFileCache[file];
+  const url = `https://raw.githubusercontent.com/${GITHUB_OWNER}/${GITHUB_REPO}/${GITHUB_BRANCH}/${file}?t=${Date.now()}`;
+  const resp = await fetch(url);
+  if(!resp.ok) throw new Error('Could not load ' + file);
+  const raw = await resp.text();
+  const source = readQuestionSource(file, raw);
+  if(!source) throw new Error('Could not find a question list in ' + file);
+  reportFileCache[file] = { raw, source };
+  return reportFileCache[file];
+}
+
+// Every reported question is pulled up front, so a collapsed card can show the
+// question itself rather than a path only its author would recognise. One
+// fetch per file, however many reports point into it.
+async function prefetchReportedQuestions(){
+  const refs = Object.keys(reportsByQuestionRef).filter(ref => !reportQ[ref]);
+  await Promise.all(refs.map(async ref => {
+    const [subjectId, moduleId, qIndexStr] = ref.split('/');
+    const qIndex = parseInt(qIndexStr, 10);
+    const found = findModuleInManifest(subjectId, moduleId);
+    if(!found){
+      reportQ[ref] = { error: 'This question belongs to a module that is no longer listed in data/subjects.json.' };
+      return;
+    }
+    try{
+      const { source } = await fetchModuleSource(found.mod.file);
+      const row = source.editable[qIndex];
+      if(!row) throw new Error('This module has no question ' + (qIndex + 1) + ' any more — the report is probably older than an edit.');
+      reportQ[ref] = {
+        file: found.mod.file,
+        qIndex,
+        row,
+        where: found.subject.name + ' \u00b7 ' + found.mod.title + ' \u00b7 Question ' + (qIndex + 1)
+      };
+    }catch(e){
+      reportQ[ref] = { error: e.message };
+    }
+  }));
 }
 
 async function openReportsTab(){
-  tabReportsEl.innerHTML = '<div class="card msg info">Loading reports…</div>';
+  tabReportsEl.innerHTML = '<div class="card msg info">Loading reports\u2026</div>';
   await loadManifest();
   await loadReports();
+  await prefetchReportedQuestions();
   renderReportsTab();
 }
 
@@ -2280,137 +2397,357 @@ function renderReportsTab(){
     tabReportsEl.innerHTML =
       '<div class="card msg error"><b>Couldn\'t load reports.</b><br>' +
       escapeHtml(reportsLoadError) +
-      '<br><br>Reports submitted from the app are being held in each reader\'s browser until this is fixed — none are lost, but none are visible here either.</div>';
+      '<br><br>Reports sent from the app are being held in each reader\'s browser until this is fixed \u2014 none are lost, but none are visible here either.</div>';
     return;
   }
-  const refs = Object.keys(reportsByQuestionRef);
-  if(refs.length === 0){
-    tabReportsEl.innerHTML = '<div class="card msg info">No open reports. Nice.</div>';
+  const allRefs = Object.keys(reportsByQuestionRef);
+  if(allRefs.length === 0){
+    tabReportsEl.innerHTML =
+      '<div class="card"><div class="empty" style="text-align:center;padding:40px 20px;color:var(--muted);font-size:14px;line-height:1.6;">' +
+      'All clear \u2014 nothing waiting.<br>Questions readers flag in the app land here.</div></div>';
     return;
   }
+
   // Newest report in each group first, so the freshest problems surface at the top.
-  refs.sort((a, b) => {
+  allRefs.sort((a, b) => {
     const la = Math.max(...reportsByQuestionRef[a].map(r => new Date(r.created_at).getTime()));
     const lb = Math.max(...reportsByQuestionRef[b].map(r => new Date(r.created_at).getTime()));
     return lb - la;
   });
 
-  tabReportsEl.innerHTML = refs.map(ref => {
-    const reports = reportsByQuestionRef[ref];
-    const [subjectId, moduleId, qIndexStr] = ref.split('/');
-    const qIndex = parseInt(qIndexStr, 10);
-    const found = findModuleInManifest(subjectId, moduleId);
-    const loc = found ? `${found.subject.name} · ${found.mod.title} · Q${qIndex + 1}` : ref;
-    const comments = reports.filter(r => r.comment).map(r => escapeHtml(r.comment));
-    return `
-      <div class="report-card" data-ref="${escapeHtml(ref)}">
-        <div class="rc-top">
-          <div class="rc-loc">${escapeHtml(loc)}</div>
-          <div class="rc-count">${reports.length} report${reports.length === 1 ? '' : 's'}</div>
-        </div>
-        <div class="rc-reasons"><b>Reasons:</b> ${escapeHtml(reasonSummary(reports))}</div>
-        ${comments.map(c => `<div class="rc-comment">"${c}"</div>`).join('')}
-        <div class="actions">
-          <button class="primary" data-action="editQ" data-ref="${escapeHtml(ref)}">Edit question</button>
-          <button class="danger" data-action="hideQ" data-ref="${escapeHtml(ref)}">Hide item</button>
-          <button data-action="dismissQ" data-ref="${escapeHtml(ref)}">Dismiss</button>
-        </div>
-        <div class="save-status" id="status-${cssEscapeRef(ref)}"></div>
-      </div>
-    `;
-  }).join('');
+  const reasons = [...new Set(allRefs.map(ref => reportsByQuestionRef[ref][0].reason))];
+  const chips = ['all'].concat(reasons).map(v =>
+    '<button class="rep-chip' + (reportFilter === v ? ' on' : '') + '" data-filter="' + escapeHtml(v) + '">' +
+    (v === 'all' ? 'All' : escapeHtml(reasonLabel(v).text)) + '</button>').join('');
+
+  const refs = allRefs.filter(ref => reportFilter === 'all' || reportsByQuestionRef[ref][0].reason === reportFilter);
+
+  tabReportsEl.innerHTML =
+    '<div class="card">' +
+      '<div class="rep-head-strip">' +
+        '<h2>' + allRefs.length + ' question' + (allRefs.length === 1 ? '' : 's') + ' need' + (allRefs.length === 1 ? 's' : '') + ' a look</h2>' +
+        '<div class="note">Fix it here, then tell the person who flagged it.</div>' +
+      '</div>' +
+      '<div class="rep-chips">' + chips + '</div>' +
+      (refs.length ? refs.map(repCardHtml).join('')
+                   : '<div style="color:var(--muted);font-size:14px;padding:20px 0;">Nothing under this filter.</div>') +
+    '</div>';
+
+  wireReportsTab();
+}
+
+function repCardHtml(ref){
+  const reports = reportsByQuestionRef[ref];
+  const info = reportQ[ref] || {};
+  const isOpen = reportOpenRef === ref;
+  const label = reasonLabel(reports[0].reason);
+  const names = [...new Set(reports.map(reporterName))];
+  const stem = info.row ? info.row.q : (info.error || ref);
+
+  return '' +
+  '<div class="rep' + (isOpen ? ' open' : '') + '" data-ref="' + escapeHtml(ref) + '">' +
+    '<div class="rep-head" data-head="' + escapeHtml(ref) + '">' +
+      '<div class="rep-caret">&#9656;</div>' +
+      '<div class="rep-main">' +
+        '<div class="rep-where">' + escapeHtml(info.where || ref) + '</div>' +
+        '<div class="rep-q">' + escapeHtml(stem) + '</div>' +
+        '<div class="rep-meta">' +
+          '<span class="rep-tag ' + label.cls + '">' + escapeHtml(label.text) + '</span>' +
+          '<span class="by">Reported by <b>' + escapeHtml(names[0]) + '</b>' +
+            (names.length > 1 ? ' and ' + (names.length - 1) + ' other' + (names.length > 2 ? 's' : '') : '') + '</span>' +
+          '<span>' + escapeHtml(shortWhen(reports[0].created_at)) + '</span>' +
+        '</div>' +
+      '</div>' +
+    '</div>' +
+    (isOpen ? repBodyHtml(ref) : '') +
+  '</div>';
+}
 
-  tabReportsEl.querySelectorAll('button[data-action="editQ"]').forEach(btn => {
-    btn.addEventListener('click', () => openReportInEditor(btn.dataset.ref));
+function repBodyHtml(ref){
+  const reports = reportsByQuestionRef[ref];
+  const info = reportQ[ref] || {};
+  const k = cssEscapeRef(ref);
+  const who = [...new Set(reports.map(reporterName))].join(' and ');
+
+  const said = reports.map(r =>
+    '<div class="rep-said">' +
+      '<span class="who">' + escapeHtml(reporterName(r)) + ', ' + escapeHtml(shortWhen(r.created_at)) +
+        ' \u00b7 ' + escapeHtml(r.reason) + '</span>' +
+      (r.comment ? escapeHtml(r.comment) : '<span style="color:var(--muted);">No comment left.</span>') +
+    '</div>').join('');
+
+  if(info.error){
+    return '<div class="rep-body">' +
+      '<div class="rep-sec"><div class="rep-sec-title">What they said</div>' + said + '</div>' +
+      '<div class="msg error" style="margin-top:18px;">' + escapeHtml(info.error) + '</div>' +
+      '<div class="rep-btns">' +
+        '<button data-act="dismiss" data-ref="' + escapeHtml(ref) + '">Dismiss</button>' +
+      '</div>' +
+      '<div class="save-status" id="status-' + k + '"></div>' +
+    '</div>';
+  }
+
+  const row = info.row;
+  const choices = (row.o || []).map((c, i) =>
+    '<div class="rep-choice' + (i === row.c ? ' correct' : '') + '">' +
+      '<input type="radio" name="rq-ans-' + k + '" value="' + i + '"' + (i === row.c ? ' checked' : '') + '>' +
+      '<input type="text" value="' + escapeHtml(c) + '">' +
+      '<span class="mark">' + (i === row.c ? 'Correct' : '') + '</span>' +
+    '</div>').join('');
+
+  return '<div class="rep-body">' +
+    '<div class="rep-sec"><div class="rep-sec-title">What they said</div>' + said + '</div>' +
+
+    '<div class="rep-sec">' +
+      '<div class="rep-sec-title">The question</div>' +
+      '<textarea id="rq-stem-' + k + '" rows="3">' + escapeHtml(row.q || '') + '</textarea>' +
+      '<p class="rep-hint">Topic shown on screen above this question: ' +
+        (row.s ? escapeHtml(row.s) : 'none') + '</p>' +
+    '</div>' +
+
+    '<div class="rep-sec">' +
+      '<div class="rep-sec-title">Choices <span>\u2014 tick the right one</span></div>' +
+      '<div id="rq-opts-' + k + '">' + choices + '</div>' +
+    '</div>' +
+
+    '<div class="rep-sec">' +
+      '<div class="rep-sec-title">Why it\'s the answer <span>(shown after they answer)</span></div>' +
+      '<textarea id="rq-note-' + k + '" rows="2" placeholder="Leave blank if there is nothing to explain.">' +
+        escapeHtml(row.n || '') + '</textarea>' +
+    '</div>' +
+
+    '<div class="rep-sec">' +
+      '<div class="rep-sec-title">Message to ' + escapeHtml(who) + ' <span>(optional)</span></div>' +
+      '<textarea id="rq-reply-' + k + '" class="rep-reply" rows="2" ' +
+        'placeholder="They see this in the app. Sent with a reply, or with Mark solved."></textarea>' +
+    '</div>' +
+
+    '<div class="rep-btns">' +
+      '<button class="primary" data-act="saveSolve" data-ref="' + escapeHtml(ref) + '">Save fix &amp; tell ' + escapeHtml(who) + '</button>' +
+      '<button data-act="save" data-ref="' + escapeHtml(ref) + '">Save without closing</button>' +
+      '<button data-act="reply" data-ref="' + escapeHtml(ref) + '">Send reply only</button>' +
+      '<span class="spacer"></span>' +
+      '<button data-act="open" data-ref="' + escapeHtml(ref) + '">Open in full editor</button>' +
+      '<button data-act="hide" data-ref="' + escapeHtml(ref) + '">Hide question</button>' +
+      '<button class="danger" data-act="dismiss" data-ref="' + escapeHtml(ref) + '">Dismiss</button>' +
+    '</div>' +
+    '<div class="save-status" id="status-' + k + '"></div>' +
+  '</div>';
+}
+
+function wireReportsTab(){
+  tabReportsEl.querySelectorAll('.rep-chip').forEach(chip => {
+    chip.addEventListener('click', () => {
+      reportFilter = chip.dataset.filter;
+      renderReportsTab();
+    });
   });
-  tabReportsEl.querySelectorAll('button[data-action="hideQ"]').forEach(btn => {
-    btn.addEventListener('click', () => hideReportedQuestion(btn.dataset.ref));
+
+  tabReportsEl.querySelectorAll('[data-head]').forEach(el => {
+    el.addEventListener('click', () => {
+      const ref = el.dataset.head;
+      reportOpenRef = (reportOpenRef === ref) ? null : ref;
+      renderReportsTab();
+    });
   });
-  tabReportsEl.querySelectorAll('button[data-action="dismissQ"]').forEach(btn => {
-    btn.addEventListener('click', () => dismissReportGroup(btn.dataset.ref));
+
+  // Ticking a different answer restages the highlight straight away, so the
+  // key you are about to save is the one you can see.
+  tabReportsEl.querySelectorAll('.rep-choice input[type="radio"]').forEach(radio => {
+    radio.addEventListener('change', () => {
+      const body = radio.closest('.rep-body');
+      body.querySelectorAll('.rep-choice').forEach(c => {
+        const on = c.querySelector('input[type="radio"]').checked;
+        c.classList.toggle('correct', on);
+        c.querySelector('.mark').textContent = on ? 'Correct' : '';
+      });
+    });
+  });
+
+  const acts = {
+    saveSolve: saveAndSolve,
+    save: saveReportedQuestion,
+    solve: markReportSolved,
+    reply: sendReportReply,
+    open: openReportInEditor,
+    hide: hideReportedQuestion,
+    dismiss: dismissReportGroup
+  };
+  tabReportsEl.querySelectorAll('button[data-act]').forEach(btn => {
+    btn.addEventListener('click', () => acts[btn.dataset.act](btn.dataset.ref));
   });
 }
 
-// "Change key" isn't a one-click action here (unlike Hide) — a report only
-// ever carries a fixed reason + free-text comment, never a structured "the
-// right answer is X" field, so there's nothing safe to act on without a
-// human reading the question. That case routes through "Edit question"
-// instead, opening the real form with this question expanded.
+function repStatus(ref, text, kind){
+  const el = document.getElementById('status-' + cssEscapeRef(ref));
+  if(!el) return;
+  el.className = 'save-status' + (kind ? ' ' + kind : '');
+  el.textContent = text;
+}
+
+async function saveReportedQuestion(ref){
+  const info = reportQ[ref];
+  if(!info || !info.row) return false;
+  const k = cssEscapeRef(ref);
+  const stem = document.getElementById('rq-stem-' + k).value.trim();
+  const note = document.getElementById('rq-note-' + k).value.trim();
+  const opts = [...document.querySelectorAll('#rq-opts-' + k + ' input[type="text"]')].map(i => i.value.trim());
+  const picked = document.querySelector('input[name="rq-ans-' + k + '"]:checked');
+
+  if(!stem){ repStatus(ref, 'The question cannot be empty.', 'err'); return false; }
+  if(opts.some(o => !o)){ repStatus(ref, 'Every choice needs some text.', 'err'); return false; }
+  if(!picked){ repStatus(ref, 'Tick which choice is the right one.', 'err'); return false; }
+
+  repStatus(ref, 'Saving\u2026');
+  try{
+    // info.row IS source.editable[qIndex], so writing here is writing into the
+    // array that gets serialised — no copy to keep in step.
+    info.row.q = stem;
+    info.row.o = opts;
+    info.row.c = Number(picked.value);
+    info.row.n = note;
+
+    const cached = reportFileCache[info.file];
+    const newText = writeQuestionSource(
+      cached.source.kind, cached.source.varName, cached.raw, cached.source.rows, cached.source.editable);
+    await callSave(info.file, newText, 'Admin: fix Q' + (info.qIndex + 1) + ' in ' + info.file + ' (from report)');
+    // Keep the cache in step with what is now on the site, so a second save in
+    // the same sitting does not rewrite from a stale copy.
+    cached.raw = newText;
+    repStatus(ref, 'Saved to the site.', 'ok');
+    return true;
+  }catch(e){
+    repStatus(ref, 'Error: ' + e.message, 'err');
+    return false;
+  }
+}
+
+// The everyday path: write the fix, then close the report and tell whoever
+// raised it. The two stay separate underneath so a failed save can never
+// send someone a "fixed" notice about a question that did not change.
+async function saveAndSolve(ref){
+  const info = reportQ[ref];
+  if(!info || !info.row) return;
+  const saved = await saveReportedQuestion(ref);
+  if(!saved) return;
+  await markReportSolved(ref);
+}
+
+// Closes the report AND tells whoever raised it. The message is whatever is in
+// the reply box, or nothing — the reader still sees that it was dealt with.
+async function markReportSolved(ref){
+  const reports = reportsByQuestionRef[ref] || [];
+  const who = [...new Set(reports.map(reporterName))].join(' and ');
+  const k = cssEscapeRef(ref);
+  const replyEl = document.getElementById('rq-reply-' + k);
+  const message = replyEl ? replyEl.value.trim() : '';
+
+  const ok = await LEAConfirm(
+    'Close ' + reports.length + ' report' + (reports.length === 1 ? '' : 's') + ' on this question and tell ' + who + ' it is fixed?',
+    { title: 'Mark solved', yes: 'Mark solved' }
+  );
+  if(!ok) return;
+
+  repStatus(ref, 'Closing\u2026');
+  try{
+    const now = new Date().toISOString();
+    const patch = { status: 'solved', resolved_at: now, seen_by_reporter: false };
+    if(message){ patch.admin_reply = message; patch.replied_at = now; }
+    const { error } = await sb.from('reports').update(patch).in('id', reports.map(r => r.id));
+    if(error) throw error;
+    reportOpenRef = null;
+    await loadReports();
+    await prefetchReportedQuestions();
+    renderReportsTab();
+  }catch(e){
+    repStatus(ref, 'Error: ' + e.message, 'err');
+  }
+}
+
+// A reply without closing anything, for when you need to ask something back.
+async function sendReportReply(ref){
+  const reports = reportsByQuestionRef[ref] || [];
+  const k = cssEscapeRef(ref);
+  const replyEl = document.getElementById('rq-reply-' + k);
+  const message = replyEl ? replyEl.value.trim() : '';
+  if(!message){ repStatus(ref, 'Write a message first.', 'err'); return; }
+
+  repStatus(ref, 'Sending\u2026');
+  try{
+    const now = new Date().toISOString();
+    const { error } = await sb.from('reports')
+      .update({ admin_reply: message, replied_at: now, seen_by_reporter: false })
+      .in('id', reports.map(r => r.id));
+    if(error) throw error;
+    repStatus(ref, 'Sent. The report stays open.', 'ok');
+  }catch(e){
+    repStatus(ref, 'Error: ' + e.message, 'err');
+  }
+}
+
+// Hiding keeps the question's slot and stops it being served, so saved
+// progress and question numbering are unaffected.
 async function hideReportedQuestion(ref){
-  const statusEl = document.getElementById('status-' + cssEscapeRef(ref));
-  const [subjectId, moduleId, qIndexStr] = ref.split('/');
-  const qIndex = parseInt(qIndexStr, 10);
-  const found = findModuleInManifest(subjectId, moduleId);
-  if(!found){ await LEAAlert('Could not find this question in the current manifest.'); return; }
+  const info = reportQ[ref];
+  if(!info || !info.row){ await LEAAlert('This question could not be loaded, so there is nothing to hide.'); return; }
   const okHide = await LEAConfirm(
-    'Hide Q' + (qIndex + 1) + ' in "' + found.mod.title + '"? It keeps its slot and stops being served — this does not delete or renumber anything, so saved progress is unaffected.',
+    'Hide question ' + (info.qIndex + 1) + '? It keeps its slot and stops being served \u2014 this does not delete or renumber anything, so saved progress is unaffected.',
     { title: 'Hide question', yes: 'Hide question' }
   );
   if(!okHide) return;
-  if(statusEl){ statusEl.className = 'save-status'; statusEl.textContent = 'Hiding…'; }
+  repStatus(ref, 'Hiding\u2026');
   try{
-    const url = `https://raw.githubusercontent.com/${GITHUB_OWNER}/${GITHUB_REPO}/${GITHUB_BRANCH}/${found.mod.file}?t=${Date.now()}`;
-    const resp = await fetch(url);
-    if(!resp.ok) throw new Error('Could not load the module file');
-    const raw = await resp.text();
-    const source = readQuestionSource(found.mod.file, raw);
-    if(!source) throw new Error('Could not find this file’s question list');
-    if(!source.rows[qIndex]) throw new Error('This module has no Q' + (qIndex + 1) + ' — the report may predate an edit.');
-    // Set on the file's own row, not the editor copy: hiding is the one edit
-    // that means the same thing in every format, so there is nothing to map.
-    source.rows[qIndex].hidden = true;
-    const newText = (source.kind === 'json')
-      ? JSON.stringify(source.rows, null, 2) + '\n'
-      : LEAQuizSource.replaceArrayLiteral(raw, source.varName, arrayToLiteralText(source.rows));
-    if(newText === null) throw new Error('Could not rewrite this file’s question list');
-    await callSave(found.mod.file, newText, `Admin: hide Q${qIndex+1} in ${found.mod.file} (from report)`);
-    if(statusEl){ statusEl.className = 'save-status ok'; statusEl.textContent = 'Hidden.'; }
+    info.row.hidden = true;
+    const cached = reportFileCache[info.file];
+    const newText = writeQuestionSource(
+      cached.source.kind, cached.source.varName, cached.raw, cached.source.rows, cached.source.editable);
+    await callSave(info.file, newText, 'Admin: hide Q' + (info.qIndex + 1) + ' in ' + info.file + ' (from report)');
+    cached.raw = newText;
+    repStatus(ref, 'Hidden.', 'ok');
   }catch(e){
-    if(statusEl){ statusEl.className = 'save-status err'; statusEl.textContent = 'Error: ' + e.message; }
+    repStatus(ref, 'Error: ' + e.message, 'err');
   }
 }
 
 function cssEscapeRef(ref){ return ref.replace(/[^a-zA-Z0-9]/g, '_'); }
 
-// Jumps to the Pages & Quizzes tab, opens the right file, and expands the
-// exact reported question — this IS the "Change key" / "Hide" path (5a):
-// both are just edits made in the normal question form, not separate
-// one-click actions, since a report only carries a reason + free-text
-// comment, never a structured "the answer should be X" field to act on
-// blindly.
+// Still here for the times a report turns out to be about the module rather
+// than the one question — it opens the whole file with this question expanded.
 function openReportInEditor(ref){
-  const [subjectId, moduleId, qIndexStr] = ref.split('/');
-  const qIndex = parseInt(qIndexStr, 10);
-  const found = findModuleInManifest(subjectId, moduleId);
-  if(!found){
-    LEAAlert('Could not find this question in the current manifest — it may belong to a module not yet listed in data/subjects.json.');
+  const info = reportQ[ref];
+  if(!info || !info.file){
+    LEAAlert('Could not find this question in the current manifest \u2014 it may belong to a module not listed in data/subjects.json.');
     return;
   }
   document.querySelector('.tab-btn[data-tab="pages"]').click();
   if(fileTree.length === 0){
-    loadFileTree().then(() => openFile(found.mod.file, qIndex));
+    loadFileTree().then(() => openFile(info.file, info.qIndex));
   } else {
-    openFile(found.mod.file, qIndex);
+    openFile(info.file, info.qIndex);
   }
 }
 
+// Dismiss leaves the question exactly as it is and closes the report unread by
+// the reader — used when the report is mistaken, not when it is fixed.
 async function dismissReportGroup(ref){
-  const statusEl = document.getElementById('status-' + cssEscapeRef(ref));
   const reports = reportsByQuestionRef[ref] || [];
   const okDismiss = await LEAConfirm(
-    'Dismiss ' + reports.length + ' report' + (reports.length === 1 ? '' : 's') + ' for this question? This marks them resolved without changing the question.',
+    'Dismiss ' + reports.length + ' report' + (reports.length === 1 ? '' : 's') + ' on this question? The question is left as it is.',
     { title: 'Dismiss reports', yes: 'Dismiss' }
   );
   if(!okDismiss) return;
-  if(statusEl){ statusEl.className = 'save-status'; statusEl.textContent = 'Dismissing…'; }
+  repStatus(ref, 'Dismissing\u2026');
   try{
-    const ids = reports.map(r => r.id);
-    const { error } = await sb.from('reports').update({ status: 'dismissed' }).in('id', ids);
+    const { error } = await sb.from('reports')
+      .update({ status: 'dismissed', resolved_at: new Date().toISOString() })
+      .in('id', reports.map(r => r.id));
     if(error) throw error;
+    reportOpenRef = null;
     await loadReports();
+    await prefetchReportedQuestions();
     renderReportsTab();
   }catch(e){
-    if(statusEl){ statusEl.className = 'save-status err'; statusEl.textContent = 'Error: ' + e.message; }
+    repStatus(ref, 'Error: ' + e.message, 'err');
   }
 }
 
diff --git a/data/theory-of-architecture-and-planning/11.json b/data/theory-of-architecture-and-planning/11.json
index 2a2e5d3..aae45b2 100644
--- a/data/theory-of-architecture-and-planning/11.json
+++ b/data/theory-of-architecture-and-planning/11.json
@@ -520,13 +520,13 @@
     "n": "Tertiary colors are created by mixing a primary color with a neighboring secondary color: Red + Orange = Red Orange, Yellow + Green = Yellow Green."
   },
   {
-    "s": "Defensible Space — Characteristic (a)",
+    "s": "Defensible Space",
     "q": "Which characteristic of a defensible-space layout involves a clear hierarchical definition of territories, from public to semi-public, semi-private to private?",
     "o": [
-      "Characteristic (b)",
-      "Characteristic (d)",
-      "Characteristic (c)",
-      "Characteristic (a)"
+      "Natural surveillance",
+      "Milieu",
+      "Image",
+      "Territoriality"
     ],
     "c": 3,
     "ref": "",
@@ -546,13 +546,13 @@
     "n": "Semi-private privacy is limited access within private settings, e.g., living rooms."
   },
   {
-    "s": "Characteristics of Defensible Space",
+    "s": "Defensible Space",
     "q": "Which characteristic of a defensible-space layout involves the positioning of doors and windows to provide natural surveillance over entrances and open spaces?",
     "o": [
-      "Characteristic (b)",
-      "Characteristic (a)",
-      "Characteristic (c)",
-      "Characteristic (d)"
+      "Natural surveillance",
+      "Territoriality",
+      "Image",
+      "Milieu"
     ],
     "c": 0,
     "ref": "",
@@ -1014,13 +1014,13 @@
     "n": "Public distance (12 to 25+ ft) covers formal speeches, public figures, or strangers."
   },
   {
-    "s": "Defensible Space — Characteristic (d)",
+    "s": "Defensible Space",
     "q": "Which characteristic of a defensible-space layout involves locating residential developments in 'functionally sympathetic' areas where residents are not threatened?",
     "o": [
-      "Characteristic (c)",
-      "Characteristic (b)",
-      "Characteristic (a)",
-      "Characteristic (d)"
+      "Image",
+      "Natural surveillance",
+      "Territoriality",
+      "Milieu"
     ],
     "c": 3,
     "ref": "",
@@ -1157,13 +1157,13 @@
     "n": "Semi-public privacy is limited to groups or members, e.g., office lounges and waiting areas."
   },
   {
-    "s": "Defensible Space — Characteristic (c)",
+    "s": "Defensible Space",
     "q": "Which characteristic of a defensible-space layout involves using building forms and materials that are not associated peculiarly with vulnerable populations?",
     "o": [
-      "Characteristic (d)",
-      "Characteristic (a)",
-      "Characteristic (c)",
-      "Characteristic (b)"
+      "Milieu",
+      "Territoriality",
+      "Image",
+      "Natural surveillance"
     ],
     "c": 2,
     "ref": "",
diff --git a/run.html b/run.html
index db34cc9..1a241cd 100644
--- a/run.html
+++ b/run.html
@@ -630,8 +630,76 @@ sb.auth.onAuthStateChange((event, session) => {
   currentUserId = session.user.id;
   if (window.LEATheme) { LEATheme.init({ altClass:'light-mode', defaultIs:'dark' }); }
   await boot();
+  checkReportUpdates();
 })();
 
+// ================= Answers to questions this reader reported =================
+// A report used to be a one-way send: you flagged a question and never heard
+// anything back. The admin panel now closes a report with a note, so the note
+// has to arrive somewhere. This runs after boot rather than blocking it — an
+// update is worth seeing, but never worth delaying the quiz for.
+async function checkReportUpdates(){
+  try{
+    const { data, error } = await sb.from('reports')
+      .select('id,question_ref,status,admin_reply,resolved_at')
+      .eq('user_id', currentUserId)
+      .eq('seen_by_reporter', false)
+      .or('admin_reply.not.is.null,resolved_at.not.is.null');
+    if(error || !data || !data.length) return;
+    showReportUpdates(data);
+  }catch(e){ /* never let this break the run */ }
+}
+
+function showReportUpdates(rows){
+  // Several reports on the same question produce several rows carrying the
+  // same note; the reader should see it once.
+  const seen = new Set();
+  const items = rows.filter(r => {
+    const key = r.question_ref + '|' + (r.admin_reply || '') + '|' + (r.status || '');
+    if(seen.has(key)) return false;
+    seen.add(key); return true;
+  });
+
+  const box = document.createElement('div');
+  box.id = 'reportUpdates';
+  box.style.cssText =
+    'position:fixed;left:50%;bottom:20px;transform:translateX(-50%);z-index:2600;' +
+    'width:min(440px, calc(100% - 24px));background:var(--bg-panel);border:1px solid var(--line);' +
+    'border-left:3px solid var(--good);border-radius:12px;padding:16px;' +
+    'box-shadow:0 8px 30px rgba(0,0,0,0.45);font-size:13.5px;line-height:1.6;color:var(--ink);';
+
+  const head = items.length === 1
+    ? 'There is news on a question you reported'
+    : 'There is news on ' + items.length + ' questions you reported';
+
+  box.innerHTML =
+    '<div style="font-weight:600;margin-bottom:10px;">' + head + '</div>' +
+    items.map(r => {
+      const fixed = r.status === 'solved';
+      return '<div style="background:var(--bg-panel-deep);border-radius:8px;padding:10px 12px;margin-bottom:8px;">' +
+        '<div style="color:' + (fixed ? 'var(--good)' : 'var(--muted)') + ';font-size:12px;margin-bottom:4px;">' +
+          (fixed ? 'Fixed' : 'Reply from the admin') + '</div>' +
+        (r.admin_reply ? escapeForUpdate(r.admin_reply)
+                       : 'This question has been corrected. Thanks for flagging it.') +
+      '</div>';
+    }).join('') +
+    '<button id="reportUpdatesOk" style="width:100%;margin-top:4px;padding:9px;border-radius:8px;' +
+      'border:none;background:var(--gold);color:var(--bg-deep);font-family:var(--font-body);' +
+      'font-weight:600;font-size:13px;cursor:pointer;">Got it</button>';
+
+  document.body.appendChild(box);
+  document.getElementById('reportUpdatesOk').addEventListener('click', async () => {
+    box.remove();
+    // Readers cannot write to reports directly, so the seen flag is flipped by
+    // a function that only ever touches their own rows.
+    try{ await sb.rpc('mark_reports_seen'); }catch(e){}
+  });
+}
+
+function escapeForUpdate(s){
+  return String(s).replace(/[&<>"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));
+}
+
 // ================= Helpers =================
 // The colour lives above every screen of the run rather than inside one,
 // because the body is rebuilt from scratch for the question, the review and
@@ -2780,12 +2848,42 @@ function svgFlame(){
   return '<svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M8.5 14.5A2.5 2.5 0 0 0 11 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 1 1-14 0c0-1.153.433-2.294 1-3a2.5 2.5 0 0 0 2.5 2.5z"/></svg>';
 }
 
+// A topic label that contains the answer is worse than no label: "Louis
+// Sullivan \u00b7 Examples" sitting above a question whose answer is "Louis
+// Sullivan" hands it over. Rather than rewriting several hundred topic strings
+// by hand \u2014 and having to police every module added afterwards \u2014 the tag is
+// checked against its own answer and dropped when it gives the game away.
+//
+// The test is on content words, so "Saleable Ratio \u00b7 PD 957" still shows above a
+// question answered by a percentage, while a topic that simply restates the
+// answer does not. Partial overlap is fine; near-total overlap is the tell.
+const TAG_STOP_WORDS = new Set(
+  'the a an of and or in on to for is are was were with by at from as which what that this it its'.split(' '));
+
+function tagWords(str){
+  return String(str || '').toLowerCase()
+    .replace(/[^a-z0-9 ]/g, ' ').replace(/\s+/g, ' ').trim()
+    .split(' ').filter(w => w.length > 3 && !TAG_STOP_WORDS.has(w));
+}
+
+function topicGivesAwayAnswer(item){
+  if(!item || !item.s || !Array.isArray(item.o)) return false;
+  const answer = item.o[item.c];
+  if(typeof answer !== 'string') return false;
+  const aw = tagWords(answer);
+  if(!aw.length) return false;
+  const tw = new Set(tagWords(item.s));
+  const shared = aw.filter(w => tw.has(w)).length;
+  return (shared / aw.length) >= 0.8;
+}
+
 // Not every module tags its questions by topic (the older QUIZ_DATA quizzes
 // carry no topic field at all). Omit the tag entirely in that case rather than
 // drawing an empty gold chip.
 function sectionTagHtml(item){
   const s = item && item.s ? String(item.s).trim() : '';
-  return s ? '<span class="section-tag">' + escapeHtml(s) + '</span>' : '';
+  if(!s || topicGivesAwayAnswer(item)) return '';
+  return '<span class="section-tag">' + escapeHtml(s) + '</span>';
 }
 
 function svgArrowLeft(){
diff --git a/subjects/building-utilities/quizzes/Utilities_AR_Annie_Exam1_Module.html b/subjects/building-utilities/quizzes/Utilities_AR_Annie_Exam1_Module.html
index 7fd6159..cf68f95 100644
--- a/subjects/building-utilities/quizzes/Utilities_AR_Annie_Exam1_Module.html
+++ b/subjects/building-utilities/quizzes/Utilities_AR_Annie_Exam1_Module.html
@@ -730,15 +730,15 @@ const QUESTIONS = [
 {"s":"Lighting Types & Elevators","q":"A standard elevator has two (2) separate door systems. What do you call the interior door which travels up and down along with the elevator and opens and closes automatically when the elevator arrives at a floor?","o":["Shaft Door","Cab Door","Landing Door","Safety Door"],"c":3},
 {"s":"Lighting Types & Elevators","q":"What do you call the exterior door of an elevator which are designed to keep people from falling into the elevator shaft when the elevator is not present.","o":["Shaft Door","Cab Door","Landing Door","Safety Door"],"c":0},
 {"s":"Lighting Types & Elevators","q":"Which component of an elevator is responsible for sealing the interior of an elevator and moves with it as it travels between floors?","o":["Hoistway Door","Shaft Door","Cab Door","Landing Door"],"c":2},
-{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, what do you call Figure A which is used in LED lighting?","o":["Driver","Transistor","Ballast","Transformer"],"c":0,"fig":"led"},
-{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, how do you use Figure B?","o":["Pool accent lighting","Spot lighting","Exterior ground lighting","Wall uplighter"],"c":2,"fig":"led"},
-{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, what is Figure C?","o":["PAR 38","PL","CFL","LED"],"c":3,"fig":"led"},
-{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, which figure or figures are energy saving lighting?","o":["Figure C only","Figure D only","Figure B and D","Figure C and D"],"c":3,"fig":"led"},
+{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, what do you call Figure A which is used in LED lighting?","o":["Driver","Transistor","Ballast","Transformer"],"c":0,"fig":"led","img":"img/q0002.png"},
+{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, how do you use Figure B?","o":["Pool accent lighting","Spot lighting","Exterior ground lighting","Wall uplighter"],"c":2,"fig":"led","img":"img/q0002.png"},
+{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, what is Figure C?","o":["PAR 38","PL","CFL","LED"],"c":3,"fig":"led","img":"img/q0002.png"},
+{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, which figure or figures are energy saving lighting?","o":["Figure C only","Figure D only","Figure B and D","Figure C and D"],"c":3,"fig":"led","img":"img/q0002.png"},
 {"s":"LED Lighting & HVAC Ducts","q":"What is the necessary component for Figure D to enable it to light up?","o":["Circuit Driver","Electronic Ballast","Circuit Transistor","Electronic Transformer"],"c":1,"fig":"led"},
-{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, what is the function of Figure A?","o":["Commercial and industrial grade exhaust duct","Removes unwanted air from inside to outside of the building","Bring in air conditioning to any part of the building","Stops or regulates the flow of air in the building"],"c":3,"fig":"duct"},
-{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, what is the function of Figure B?","o":["Solar Attic Fan","Sun Roof","Electric Vent","Solar Lighting"],"c":0,"fig":"duct"},
-{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, it is a device installed at the end of ductwork to distribute conditioned air evenly throughout a room","o":["HVAC Damper","Return Air Vent","Air Duct Cover","Air Diffuser"],"c":3,"fig":"duct"},
-{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, what is the function of Figure D?","o":["Transformer","Air Interchanger","Generator","Air-conditioner"],"c":1,"fig":"duct"},
+{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, what is the function of Figure A?","o":["Commercial and industrial grade exhaust duct","Removes unwanted air from inside to outside of the building","Bring in air conditioning to any part of the building","Stops or regulates the flow of air in the building"],"c":3,"fig":"duct","img":"img/q0003.png"},
+{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, what is the function of Figure B?","o":["Solar Attic Fan","Sun Roof","Electric Vent","Solar Lighting"],"c":0,"fig":"duct","img":"img/q0003.png"},
+{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, it is a device installed at the end of ductwork to distribute conditioned air evenly throughout a room","o":["HVAC Damper","Return Air Vent","Air Duct Cover","Air Diffuser"],"c":3,"fig":"duct","img":"img/q0003.png"},
+{"s":"LED Lighting & HVAC Ducts","q":"From the figures above, what is the function of Figure D?","o":["Transformer","Air Interchanger","Generator","Air-conditioner"],"c":1,"fig":"duct","img":"img/q0003.png"},
 {"s":"LED Lighting & HVAC Ducts","q":"Which of the following is an effective and specialized solution for reducing noise transmission in air ducts?","o":["Increasing duct diameter to lower air velocity","Using a sound attenuator to specifically dampen noise","Adding insulation around ducts to absorb sound and reduce vibration","Installing airflow dampers to regulate pressure changes"],"c":1},
 {"s":"Brands & Fixture Boxes","q":"Brand name for bathtub manufacturer","o":["Emerald","Koten","Hafele","HoeCheng Group"],"c":3},
 {"s":"Brands & Fixture Boxes","q":"Brand name for Faucets","o":["Grohe","Delta","Moen","All of the above"],"c":3},
@@ -750,11 +750,11 @@ const QUESTIONS = [
 {"s":"Brands & Fixture Boxes","q":"It allows long wiring runs to be installed in shorter intervals and does not have wiring connections inside.","o":["Pullout Box","Junction Box","NEMA Box","Panel Box"],"c":0},
 {"s":"Brands & Fixture Boxes","q":"Which should I use as a conduit for telephone wires?","o":["EMT","IMC","PVC","None"],"c":1},
 {"s":"Brands & Fixture Boxes","q":"An electrical fixture box that protects electrical wires at their splicing and tapping points.","o":["Pullout Box","Junction Box","NEMA Box","Panel Box"],"c":1},
-{"s":"Drain Fixtures & Elevator Indicators","q":"From the figures above, which one is used at the driveway or garage?","o":["A","B","C","D"],"c":2,"fig":"drainfix"},
-{"s":"Drain Fixtures & Elevator Indicators","q":"From the figures above, which one is used at the bathroom shower area?","o":["B","C","D","E"],"c":2,"fig":"drainfix"},
-{"s":"Drain Fixtures & Elevator Indicators","q":"From the figures above, which one is used at the gutter?","o":["A","B","C","E"],"c":1,"fig":"drainfix"},
-{"s":"Drain Fixtures & Elevator Indicators","q":"From the figures above, which one is used at the roof deck parapet?","o":["A","B","C","D"],"c":0,"fig":"drainfix"},
-{"s":"Drain Fixtures & Elevator Indicators","q":"From the figures above, which one is used at hard to reach areas?","o":["B","C","D","E"],"c":3,"fig":"drainfix"},
+{"s":"Drain Fixtures & Elevator Indicators","q":"From the figures above, which one is used at the driveway or garage?","o":["A","B","C","D"],"c":2,"fig":"drainfix","img":"img/q0004.png"},
+{"s":"Drain Fixtures & Elevator Indicators","q":"From the figures above, which one is used at the bathroom shower area?","o":["B","C","D","E"],"c":2,"fig":"drainfix","img":"img/q0004.png"},
+{"s":"Drain Fixtures & Elevator Indicators","q":"From the figures above, which one is used at the gutter?","o":["A","B","C","E"],"c":1,"fig":"drainfix","img":"img/q0004.png"},
+{"s":"Drain Fixtures & Elevator Indicators","q":"From the figures above, which one is used at the roof deck parapet?","o":["A","B","C","D"],"c":0,"fig":"drainfix","img":"img/q0004.png"},
+{"s":"Drain Fixtures & Elevator Indicators","q":"From the figures above, which one is used at hard to reach areas?","o":["B","C","D","E"],"c":3,"fig":"drainfix","img":"img/q0004.png"},
 {"s":"Drain Fixtures & Elevator Indicators","q":"Also called position indicators, they are used to notify elevator riders that their elevator is arriving and what direction it will be traveling.","o":["Up/Down Button","Hall Indicator (hall lantern)","Call Button","Floor Designation Buttons"],"c":0},
 {"s":"Wattage & Green Building Standards","q":"Wattage for Airconditioner","o":["800-2000","1000-2500","3000-4500","3500-6000"],"c":2},
 {"s":"Wattage & Green Building Standards","q":"Wattage for Single Point Water Heater","o":["800-2000","1000-2500","3000-4500","3500-6000"],"c":3},
@@ -782,20 +782,20 @@ const QUESTIONS = [
 {"s":"Acoustic Insulation Materials","q":"Identify from among the illustrations a Mineral Wool","o":["FIGURE D","FIGURE A","FIGURE B","FIGURE C"],"c":3,"fig":"acoustic"},
 {"s":"Acoustic Insulation Materials","q":"Identify from among the illustrations a Glass Wool","o":["FIGURE D","FIGURE A","FIGURE B","FIGURE C"],"c":1,"fig":"acoustic"},
 {"s":"Acoustic Insulation Materials","q":"Identify from among the illustrations a Drywall","o":["FIGURE D","FIGURE A","FIGURE B","FIGURE C"],"c":0,"fig":"acoustic"},
-{"s":"Lighting Fixture Types","q":"From the figures above, which will you use for 8m ceilings?","o":["A","B","C","E"],"c":1,"fig":"lightfix"},
-{"s":"Lighting Fixture Types","q":"From the figures above, which lighting is most efficient?","o":["B","C","D","E"],"c":2,"fig":"lightfix"},
-{"s":"Lighting Fixture Types","q":"From the figures above, which will you use for wide area?","o":["B","C","D","E"],"c":3,"fig":"lightfix"},
-{"s":"Lighting Fixture Types","q":"From the figures above, which lighting is suitable for outdoor commercial purposes?","o":["B","C","D","E"],"c":2,"fig":"lightfix"},
-{"s":"Lighting Fixture Types","q":"From the figures above, which is required by the building code?","o":["A","B","C","D"],"c":0,"fig":"lightfix"},
-{"s":"Lighting Fixture Types","q":"From the figures above, which will you use to highlight a building?","o":["A","B","D","E"],"c":2,"fig":"lightfix"},
-{"s":"Lighting Fixture Types","q":"From the figures above, which lighting has built-in batteries?","o":["A","B","C","D"],"c":0,"fig":"lightfix"},
-{"s":"Lighting Fixture Types","q":"From the figures above, which is used as accent light?","o":["B","C","D","E"],"c":0,"fig":"lightfix"},
-{"s":"Lighting Fixture Types","q":"From the figures above, which will you least use in an office?","o":["A","B","C","D"],"c":2,"fig":"lightfix"},
-{"s":"Lighting Fixture Types","q":"From the figure above, which is used as a lighting for a supermarket?","o":["B","C","D","E"],"c":3,"fig":"lightfix"},
-{"s":"Lighting Fixture Types","q":"From the figures above, which is used at stairs?","o":["A","B","C","D"],"c":0,"fig":"lightfix"},
-{"s":"Lighting Fixture Types","q":"From the figures above, which is used for the exterior?","o":["A","B","C","D"],"c":3,"fig":"lightfix"},
-{"s":"Lighting Fixture Types","q":"From the figures above, which are used at bar lounge/clubs?","o":["D and E","B and C","B and D","C and E"],"c":1,"fig":"lightfix"},
-{"s":"Lighting Fixture Types","q":"From the figures above, which are used at warehouses?","o":["A and B","A and C","A and D","A and E"],"c":3,"fig":"lightfix"},
+{"s":"Lighting Fixture Types","q":"From the figures above, which will you use for 8m ceilings?","o":["A","B","C","E"],"c":1,"fig":"lightfix","img":"img/q0007.png"},
+{"s":"Lighting Fixture Types","q":"From the figures above, which lighting is most efficient?","o":["B","C","D","E"],"c":2,"fig":"lightfix","img":"img/q0007.png"},
+{"s":"Lighting Fixture Types","q":"From the figures above, which will you use for wide area?","o":["B","C","D","E"],"c":3,"fig":"lightfix","img":"img/q0007.png"},
+{"s":"Lighting Fixture Types","q":"From the figures above, which lighting is suitable for outdoor commercial purposes?","o":["B","C","D","E"],"c":2,"fig":"lightfix","img":"img/q0007.png"},
+{"s":"Lighting Fixture Types","q":"From the figures above, which is required by the building code?","o":["A","B","C","D"],"c":0,"fig":"lightfix","img":"img/q0007.png"},
+{"s":"Lighting Fixture Types","q":"From the figures above, which will you use to highlight a building?","o":["A","B","D","E"],"c":2,"fig":"lightfix","img":"img/q0007.png"},
+{"s":"Lighting Fixture Types","q":"From the figures above, which lighting has built-in batteries?","o":["A","B","C","D"],"c":0,"fig":"lightfix","img":"img/q0007.png"},
+{"s":"Lighting Fixture Types","q":"From the figures above, which is used as accent light?","o":["B","C","D","E"],"c":0,"fig":"lightfix","img":"img/q0007.png"},
+{"s":"Lighting Fixture Types","q":"From the figures above, which will you least use in an office?","o":["A","B","C","D"],"c":2,"fig":"lightfix","img":"img/q0007.png"},
+{"s":"Lighting Fixture Types","q":"From the figure above, which is used as a lighting for a supermarket?","o":["B","C","D","E"],"c":3,"fig":"lightfix","img":"img/q0007.png"},
+{"s":"Lighting Fixture Types","q":"From the figures above, which is used at stairs?","o":["A","B","C","D"],"c":0,"fig":"lightfix","img":"img/q0007.png"},
+{"s":"Lighting Fixture Types","q":"From the figures above, which is used for the exterior?","o":["A","B","C","D"],"c":3,"fig":"lightfix","img":"img/q0007.png"},
+{"s":"Lighting Fixture Types","q":"From the figures above, which are used at bar lounge/clubs?","o":["D and E","B and C","B and D","C and E"],"c":1,"fig":"lightfix","img":"img/q0007.png"},
+{"s":"Lighting Fixture Types","q":"From the figures above, which are used at warehouses?","o":["A and B","A and C","A and D","A and E"],"c":3,"fig":"lightfix","img":"img/q0007.png"},
 {"s":"Lighting Fixture Types","q":"A kind of lighting designed for both indoor and outdoor applications. Also known as tape or ribbon lights, it is very flexible and can be easily cut into the desired length.","o":["Recessed Light","Flood Light","Strip Light","Emergency Light"],"c":2},
 {"s":"Air Conditioning & Ventilation","q":"Process of treating air to control its temperature","o":["Refrigeration","Cooling","Air Conditioning","Heating"],"c":2},
 {"s":"Air Conditioning & Ventilation","q":"Installed 2.10m on top of a wall","o":["WACU","Diffuser","Ceiling Register","Wall Register"],"c":0},
diff --git a/sw.js b/sw.js
index 7cdac71..c7a9b09 100644
--- a/sw.js
+++ b/sw.js
@@ -30,7 +30,7 @@
 // the last change (the subject list moving to network-first) shipped without
 // one, which left the old copy sitting in the old cache on every device that
 // had visited before.
-const VERSION = 'lea-v6';
+const VERSION = 'lea-v7';
 const SHELL = VERSION + '-shell';
 const DATA = VERSION + '-data';
 const VENDOR = VERSION + '-vendor';
